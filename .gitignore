# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# uv: the lockfile must be committed for reproducibility, do not ignore it.
# .python-version too (managed by uv, useful for pinning).

# Tooling
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# Env / secrets
.env
.env.local

# Data — kept out of git, populated by ingestion scripts
data/raw/
data/processed/
data/models/

# Eval reports
eval/reports/

# Inference model cache (mlx-lm pulls from HF)
.cache/huggingface/

# Local docker volumes
docker/volumes/

# Local-only docker compose overrides (e.g. host port remaps when other
# stacks occupy the canonical ports). Compose auto-merges this file.
docker-compose.override.yml
